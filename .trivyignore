# Trivy image-scan baseline (advisory only — see .github/workflows/security.yaml,
# job "trivy"). Currently empty.
#
# Add CVE IDs here (one per line, with a comment explaining why it's
# accepted) if/when the scan starts finding vulnerabilities we can't fix
# immediately. Once this stays clean in CI for a while, flip the trivy job
# from advisory (continue-on-error + exit-code 0) to blocking.
