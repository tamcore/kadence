apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kadence.fullname" . }}-secret
  labels:
    {{- include "kadence.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{- if $value }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}
